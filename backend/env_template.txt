# 🔧 Human Record - Environment Variables Setup
# 
# Follow these steps to configure your .env file:
#
# 1. SUPABASE SETUP:
#    - Go to https://supabase.com and create a project
#    - Run the SQL from database/schema.sql in SQL Editor
#    - Create storage bucket named "audio-recordings"
#    - Go to Settings → API and copy these values:
#
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_anon_public_key_here
SUPABASE_SERVICE_KEY=your_service_role_key_here
#
# 2. GOOGLE OAUTH SETUP:
#    - Go to https://console.cloud.google.com
#    - Create a project and enable Google+ API
#    - Go to APIs & Services → Credentials
#    - Create OAuth 2.0 Client ID (Web application)
#    - Add redirect URI: http://localhost:8000/api/v1/auth/google/callback
#    - Copy these values:
#
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback
#
# 3. JWT SECRET (generate a random string):
#
JWT_SECRET_KEY=your_random_jwt_secret_here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
#
# 4. APPLICATION CONFIGURATION:
#
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
UPLOAD_DIR=uploads
#
# 5. Copy this content to your .env file and replace the placeholder values

